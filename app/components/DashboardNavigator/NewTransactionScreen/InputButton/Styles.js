import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  inputButton: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  inputButtonText: {
    fontSize: 40,
    color: '#FFFFFF',
  }
});
